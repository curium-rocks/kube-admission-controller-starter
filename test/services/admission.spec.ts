import { describe, it } from '@jest/globals'

describe('services/admission', () => {
  it('Should block images on disallow list', () => {

  })
  it('Should allow images not ondisallow list', () => {

  })
  it('Should only allow images on allow list when in restrictive mode', () => {

  })
})
